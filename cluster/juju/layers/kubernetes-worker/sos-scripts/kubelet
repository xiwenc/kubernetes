#!/bin/sh
set -ux

systemctl status kubelet > $SOS_SCRIPT_DIR/kubelet-systemctl-status
journalctl -u kubelet > $SOS_SCRIPT_DIR/kubelet-journal

mkdir -p $SOS_SCRIPT_DIR/etc-default
cp /etc/default/kube* $SOS_SCRIPT_DIR/etc-default

mkdir -p $SOS_SCRIPT_DIR/lib-systemd-system
cp /lib/systemd/system/kube* $SOS_SCRIPT_DIR/lib-systemd-system
