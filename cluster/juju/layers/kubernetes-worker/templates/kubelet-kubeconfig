apiVersion: v1
kind: Config
clusters:
- name: local
  cluster:
    {%- if ssl_path %}
    certificate-authority: {{ ssl_path }}/ca.crt
    server: {{ kube_api_endpoint }}
    {% else %}
    server: {{ kube_api_endpoint }}
    {% endif %}
users:
- name: kubelet
  user:
  {%- if ssl_path %}
    client-certificate: {{ ssl_path }}/client.crt
    client-key: {{ ssl_path }}/client.key
  {% endif %}
contexts:
- context:
    cluster: local
    user: kubelet
  name: kubelet-context
current-context: kubelet-context
